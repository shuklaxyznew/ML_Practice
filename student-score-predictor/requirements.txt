# Student Score Prediction System
# Python 3.10+
# ============================================================

# ── Core ML ─────────────────────────────────────────────────
tensorflow>=2.13.0,<3.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# ── Explainability ──────────────────────────────────────────
shap>=0.43.0

# ── HTTP (used by all free LLM backends) ────────────────────
requests>=2.31.0

# ── LLM Backends (install only what you use) ────────────────
#
# Ollama  → no Python package needed, just the server (https://ollama.ai)
# Groq    → no extra package needed (uses raw requests)
# HuggingFace → no extra package needed (uses raw requests)
# Gemini  → no extra package needed (uses raw requests)
# Anthropic (paid):
# anthropic>=0.25.0

# ── Serving ─────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.0.0

# ── Experiment Tracking ─────────────────────────────────────
mlflow>=2.11.0

# ── Visualization ───────────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0

# ── Dev / Testing ───────────────────────────────────────────
pytest>=7.4.0
pytest-cov>=4.1.0
jupyter>=1.0.0
ipykernel>=6.25.0

# ── Utilities ───────────────────────────────────────────────
python-dotenv>=1.0.0
tqdm>=4.66.0
